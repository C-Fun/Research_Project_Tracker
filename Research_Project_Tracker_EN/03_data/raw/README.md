# raw data — read-only

**Nothing in this folder should ever be modified.**

- All files are read-only
- Data sources and versions are documented in [[source_log]]
- All intermediate data goes in `../processed/` and must be generated by code
